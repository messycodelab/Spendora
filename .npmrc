# pnpm configuration
auto-install-peers=true
shamefully-hoist=false
strict-peer-dependencies=false

# Electron specific - allow build scripts
enable-pre-post-scripts=true

# Allow build scripts for native modules
enable-build-scripts=true

# Automatically allow build scripts for these packages
unsafe-perm=true
